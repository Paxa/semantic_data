%article[post]{:itemprop => "blogPosts"}

  = link_to post, :itemprop => "url" do
    %h3[:name]>= post.title
  
  .post_body[:articleBody]= post.body.html_safe
  
  %time{:datetime => post.published_at.iso8601(10), :itemprop => "datePublished" }= post.published_at.strftime("%d %h %Y")